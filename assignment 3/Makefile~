#
# example Makefile to build an executable named myprog from myprog.cpp
#
PROG=sieve_of_ero, matrix_mult

all: $(PROG).cpp
	g++ -std=c++11 -Wall -fopenmp $(PROG).cpp -o $(PROG)

clean:
	$(RM) $(PROG)
